[package]
name = "atm-refraction"
version = "0.2.2"
authors = ["Bartłomiej Kamiński <fizyk20@gmail.com>"]

[dependencies]
numeric-algs = { path = "../numeric-algs" }
clap = "2.*"
nom = "4.*"
regex = "*"

[features]
default = ["nom/regexp"]
